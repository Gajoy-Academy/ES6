// objetos
const pessoa = {
    nome: 'Gabe',
    idade: 22,
    stack: "Backend"
};

const { nome, idade, stack } = pessoa;
console.log(nome, idade, stack);

// arrays
const idades = [10, 20, 30, 50, 70];

const [joao, maria, jorge, ze, margarida] = idades;
